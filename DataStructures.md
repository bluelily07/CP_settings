*cf handle: bluelily07*

## Базовые структуры (кратко)

**stack**

элементы добавляются сверху как стопка книг, выходят оттуда посредством снятия сверху, то есть взять элемент можно только последний добавленный (LIFO)

**queue**

элементы добавляются сверху как стопка книг, но выходит первый добавленный, а не последний (FIFO)

**deque**

теперь элементы можно добавлять не только сверху, но еще и снизу. Удалять элементы так же можно как снизу, так и сверху.

**set** 

сбалансированное дерево, хранящее только уникальные элементы отсортированными. Все операции
<!--stackedit_data:
eyJoaXN0b3J5IjpbMTI5NjM1MzE0MF19
-->